module.exports = {
  testEnvironment: 'node', // Use the Node.js environment
  testMatch: ['**/__tests__/**/*.js?(x)', '**/?(*.)+(spec|test).js?(x)'], // Look for tests with these file name patterns
  verbose: true, // Show detailed output
};